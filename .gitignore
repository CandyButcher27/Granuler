# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Virtual environment
.venv/

# Environment variables / secrets
.env
.env.*

# Generated output PPTX (not the template)
*_Granuler_Assessment.pptx

# Test artifacts
*.pptx
# ...but never the branded master template: it must ship to the deployed
# container. `railway up` honours these patterns, so a bare *.pptx strips it
# from the upload and /generate-report 500s with PackageNotFoundError.
!assets/granuler_template.pptx

# Claude / personal
CLAUDE.md
.claude/
interview_prep_granuler.md
test_pptx.py
GOAL.md
MISTAKES.md
memory/
